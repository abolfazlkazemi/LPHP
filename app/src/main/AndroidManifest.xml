<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lphp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".fehrest_activity" />
        <activity android:name=".fasl1_activity" />
        <activity android:name=".fasl2_activity" />
        <activity android:name=".fasl3_activity" />
        <activity android:name=".fasl4_activity" />
        <activity android:name=".fasl5_activity" />
    </application>

</manifest>